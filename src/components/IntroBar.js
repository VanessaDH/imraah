import "./styles/IntroBar.css"

function IntroBar(){
    return(
        <div className="intro-bar">
            <div>International Media Resources, Applications & Aggregation Hub</div>
        </div>
    );
}

export default IntroBar;
